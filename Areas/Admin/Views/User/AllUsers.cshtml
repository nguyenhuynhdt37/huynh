@model IEnumerable<AppUserModel>
@{
    ViewData["Title"] = "Tất cả người dùng";
}

<main id="main" class="main"> 
    <div class="pagetitle">
        <h2>Danh sách người dùng</h2>

        <p>
            <a asp-area="Admin" asp-controller="User" asp-action="Create" type="button" class="btn btn-success">
            <i class="bi bi-file-earmark-text me-1"></i>Add new user</a>
            <section class="section dashboard">
                <div class="row">
                    <div class="col-12">
                        <div class="card recent-sales overflow-auto">
                            <div class="card-body mt-4">
                                <table class="table table-borderless datatable">
                                    <thead>
                                        <tr>
                                            <th class="col-1">Avatar</th>
                                            <th class="col-3">UserID</th>
                                            <th class="col-3">Username</th>
                                            <th class="col-3">Email</th>
                                            <th class="col-2">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td class="text-left " scope="row">
                                                    <img src="@item.Avatar" alt="Avatar" class="img-fluid rounded-2 border" style="width: 50px; height: 50px;" />
                                                </td>
                                                <td class="text-left" scope="row">@item.Id</td>
                                                <td class="text-left" scope="row">@item.UserName</td>
                                                <td class="text-left" scope="row">@item.Email</td>
                                                <td class="text-left" scope="row">
                                                    <a asp-area="Admin" asp-controller="User" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary btn-sm" title="Edit this user">
                                                        <i class="bi bi-pencil"></i>
                                                    </a>
                                                    <a asp-area="Admin" asp-controller="User" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm" title="Delete this user">
                                                        <i class="bi bi-trash"></i>
                                                    </a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </p>
    </div>
</main>